using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

/// <summary>
/// TagLayer的帮助类。
/// 我TagLayer的使用似乎弄错了，暂时不改，要改也要先把现有的所有的都集中起来
/// </summary>
public static class TagLayerHelper
{
    //public static bool IsEnvironment(int layer)
    //{
    //    return layer == LayerMask.NameToLayer(Layers.Floor) || layer == LayerMask.NameToLayer(Layers.Wall);
    //    //return false;
    //}
}
